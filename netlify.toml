# Configuration pour le déploiement sur Netlify

[build]
  # Commande de build
  command = "npm run build"

  # Répertoire de publication pour l'export statique
  publish = "out"

  # Node.js version
  node_version = "18"
